# `ds_msp.detect`

Detection adapters (checkerboard / ChArUco / AprilGrid) — the OpenCV-facing layer that turns
raw images into 3D↔2D correspondences. See
[detecting every AprilGrid tag](../learn/robust_aprilgrid_detection.md) for the multi-scale
detection deep-dive.

::: ds_msp.detect
