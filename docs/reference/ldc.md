# `ds_msp.ldc`

Lens-distortion-correction mesh export — for embedded/ISP undistortion pipelines that need a
precomputed remap mesh rather than a per-frame closed-form call. See
[Export an LDC mesh](../how-to/export_ldc_mesh.md).

::: ds_msp.ldc
